import { Check, Clock3, Minus } from 'lucide-react'; import type { FollowUpState } from '@/lib/event-types';
export function FollowUpBadge({ state }: { state: FollowUpState }) { const config = state === 'done' ? { label: 'Sudah follow-up', icon: Check, style: 'bg-emerald-50 text-emerald-700 ring-emerald-200' } : state === 'pending' ? { label: 'Belum follow-up', icon: Clock3, style: 'bg-amber-50 text-amber-700 ring-amber-200' } : { label: 'Tidak perlu', icon: Minus, style: 'bg-slate-50 text-slate-600 ring-slate-200' }; const Icon = config.icon; return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-semibold ring-1 ring-inset ${config.style}`}><Icon size={12}/>{config.label}</span>; }
